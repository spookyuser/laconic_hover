{
  "private": true,
  "version": "1.5.0",
  "scripts": {
    "dev": "parcel watch source/manifest.json --host localhost --config @parcel/config-webextension",
    "build": "parcel build source/manifest.json --config @parcel/config-webextension --no-source-maps",
    "save:version": "dot-json dist/manifest.json version $npm_package_version",
    "package": "npm run build && npm run save:version && zip -r dist/extension.zip dist/* -x dist/extension.zip"
  },
  "dependencies": {
    "common-tags": "^1.8.2",
    "tippy.js": "^6.3.7"
  },
  "devDependencies": {
    "@types/common-tags": "^1.8.1",
    "@parcel/config-webextension": "^2.8.3",
    "@parcel/core": "^2.8.3",
    "dot-json": "^1.2.2",
    "typescript": "^4.9.4"
  },
  "prettier": {
    "singleQuote": false,
    "endOfLine": "auto",
    "trailingComma": "es5",
    "bracketSpacing": true,
    "useTabs": false
  }
}