{
  "name": "LaconicHover",
  "version": "0.0.5",
  "description": "Displays laconic text when hovering over a tvtropes link",
  "dependencies": {
    "jquery": "2.2.2",
    "qtip2": "^3.0.3"
  },
  "devDependencies": {
    "archiver": "^1.3.0",
    "chrome-extension-deploy": "^2.0.3",
    "copy-webpack-plugin": "^4.0.1",
    "css-loader": "^0.26.1",
    "extract-text-webpack-plugin": "^1.0.1",
    "firefox-extension-deploy": "0.0.2",
    "jshint": "latest",
    "rimraf": "^2.5.4",
    "style-loader": "^0.13.1",
    "uglify-js": "^2.7.5",
    "webpack": "^1.14.0"
  },
  "scripts": {
    "test": "npm run lint",
    "lint": "jshint ./src/js/",
    "build": "npm run clean && webpack --optimize-minimize && npm run zip",
    "clean": "rimraf build && rimraf ./dist/* && mkdir build",
    "zip": "node ./tools/zip-dist.js",
    "deploy:chrome": "node ./tools/chrome-deploy.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}
